# ============================================
# SOUNDSHARE ENVIRONMENT VARIABLES
# ============================================
# Copy this file to .env.local and fill in your values

# --------------------------------------------
# APP CONFIGURATION
# --------------------------------------------
# Your production URL (e.g., https://soundshare.app)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# JWT secret for session tokens (generate a secure random string)
# Generate with: openssl rand -base64 32
JWT_SECRET=your-secret-key-change-this-in-production

# --------------------------------------------
# DATABASE (SQLite for local, PostgreSQL for production)
# --------------------------------------------
# For production, use PostgreSQL:
# DATABASE_URL="postgresql://user:password@localhost:5432/soundshare"

# --------------------------------------------
# EMAIL SERVICE - OPTION 1: RESEND (Recommended)
# --------------------------------------------
# Sign up at https://resend.com (free tier: 100/day, 3000/month)
# 1. Create account at https://resend.com
# 2. Get API key from dashboard
# 3. Optionally add your domain for better deliverability

RESEND_API_KEY=re_xxxxxxxxxxxxx
EMAIL_FROM=hello@yourdomain.com

# --------------------------------------------
# EMAIL SERVICE - OPTION 2: SMTP (Gmail, SendGrid, etc)
# --------------------------------------------
# For Gmail:
# 1. Enable 2FA on your Google account
# 2. Generate app password: https://myaccount.google.com/apppasswords
# 3. Use your email as SMTP_USER and app password as SMTP_PASS

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# For SendGrid:
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASS=your-sendgrid-api-key

# --------------------------------------------
# FILE STORAGE (Future)
# --------------------------------------------
# For production, you'll want to use cloud storage instead of local files
# Options: Cloudflare R2, AWS S3, Vercel Blob, etc.

# Cloudflare R2 (S3-compatible, no egress fees)
# R2_ACCOUNT_ID=
# R2_ACCESS_KEY_ID=
# R2_SECRET_ACCESS_KEY=
# R2_BUCKET_NAME=

# --------------------------------------------
# OPTIONAL: ANALYTICS & MONITORING
# --------------------------------------------
# VERCEL_ANALYTICS_ID=
# SENTRY_DSN=
# POSTHOG_API_KEY=